<% @articles.each do |article| %>
  <h2> author: <%= link_to article.title, article_path(article.id) %></h2>
  <p><%= article.content %></p>
  <br>
<% end %>
<button><%= link_to "New Tweet", new_article_path %></button>
